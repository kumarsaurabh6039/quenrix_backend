# csmitbackend
This is new repository for backend

# first clone the repository in your system

# to install all the requirements ->
pip install -r requirements.txt

# go inside the project and run command -> 
python manage.py runserver

# to work on this project you need to create ->
.env file  in projects main folder

# in that file provide openai key with variable ->
OPENAI_API_KEY = 

